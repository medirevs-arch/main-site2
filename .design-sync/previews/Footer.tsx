import { Footer } from "medirevs-v2";

/**
 * The site footer, whole: newsletter band, four link columns built from
 * PRODUCTS in src/lib/site.ts, contact block and legal rule. It takes no
 * props and carries its own dark surface and `.shell` width.
 */
export const Default = () => <Footer />;
