import { LabsReveal } from "medirevs-v2";

/**
 * The transition into Medirevs Labs: the page inverts to the laboratory
 * palette and the microscope loop is held inside a circular aperture — the
 * same optical geometry used across the site, here doing the work of a
 * doorway. Content comes from LABS in src/lib/site.ts.
 */
export const Default = () => <LabsReveal />;
